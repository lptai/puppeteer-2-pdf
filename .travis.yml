language: node_js
node_js:
  - 'stable'
cache:
  directories:
    - node_modules
before_script:
  - rm -rf build
script:
  - cd webapp
  - yarn install
  - yarn build
  - cd ..
deploy:
  provider: heroku
  skip_cleanup: true
  local_dir: build
  api_key: ${api_key}